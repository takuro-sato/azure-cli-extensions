name: Region (Basic) - Poland Central

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '40 2 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-polandcentral.yml

jobs:
  basic-polandcentral:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: polandcentral
