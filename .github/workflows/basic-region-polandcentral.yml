name: Region (Basic) - Poland Central

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '16 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-polandcentral:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: polandcentral
