name: Perf - South Central US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '35 7 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/perf-southcentralus.yml

jobs:
  perf-southcentralus:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: southcentralus
