name: High Spec - Italy North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/high-spec-italynorth.yml
      - .github/workflows/high-spec.yml

jobs:
  high-spec-italynorth:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: italynorth
