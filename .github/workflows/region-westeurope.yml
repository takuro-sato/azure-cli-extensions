name: Region - West Europe

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '44 14 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  westeurope:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: westeurope
