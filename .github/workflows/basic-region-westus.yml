name: Region (Basic) - West US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '24 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-westus:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: westus
