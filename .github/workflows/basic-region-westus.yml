name: Region (Basic) - West US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-westus.yml

jobs:
  basic-westus:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: westus
