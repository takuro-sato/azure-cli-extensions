name: High Spec - East US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/high-spec-eastus.yml

jobs:
  high-spec-eastus:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: eastus
