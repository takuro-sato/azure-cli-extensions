name: Region (Basic) - Chile Central

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-chilecentral.yml

jobs:
  basic-chilecentral:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: chilecentral
