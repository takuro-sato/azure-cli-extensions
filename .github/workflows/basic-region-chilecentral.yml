name: Region (Basic) - Chile Central

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '3,18,33,48 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-chilecentral:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: chilecentral
