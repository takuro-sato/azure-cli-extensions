name: Attestation - Korea Central

permissions:
  id-token: write
  contents: read

on:
  pull_request:
    paths:
      - .github/workflows/attestation-koreacentral.yml
  schedule:
    - cron: '26 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  attestation-koreacentral:
    secrets: inherit
    uses: ./.github/workflows/workload-attestation.yml
    with:
      location: koreacentral
      policy_type: allow_all
      test_schedule: gh-hourly
