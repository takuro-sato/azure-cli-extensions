name: Region (Basic) - Japan East

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-japaneast.yml

jobs:
  basic-japaneast:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: japaneast
