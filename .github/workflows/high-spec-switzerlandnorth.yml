name: High Spec - Switzerland North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/high-spec-switzerlandnorth.yml
      - .github/workflows/high-spec.yml

jobs:
  high-spec-switzerlandnorth:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: switzerlandnorth
