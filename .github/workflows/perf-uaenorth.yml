name: Perf - UAE North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '55 8 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  perf-uaenorth:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: uaenorth
