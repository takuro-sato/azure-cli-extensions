name: High Spec - Sweden Central

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '35 23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  high-spec-swedencentral:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: swedencentral
