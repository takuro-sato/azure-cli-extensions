name: Perf - Malaysia West

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '6 3 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  perf-malaysiawest:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: malaysiawest
