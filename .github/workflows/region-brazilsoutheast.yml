name: Region - Brazil Southeast

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '24 5 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  brazilsoutheast:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: brazilsoutheast
