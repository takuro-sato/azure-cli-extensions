name: Region (Basic) - France Central

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '11 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-francecentral:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: francecentral
