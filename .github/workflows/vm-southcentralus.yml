name: VM - South Central US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/vm-southcentralus.yml

jobs:
  vm-southcentralus:
    secrets: inherit
    uses: ./.github/workflows/vm-region.yml
    with:
      location: southcentralus
