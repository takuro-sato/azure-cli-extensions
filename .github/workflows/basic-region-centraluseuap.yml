name: Region (Basic) - Central US EUAP

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '2 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-centraluseuap:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: centraluseuap
