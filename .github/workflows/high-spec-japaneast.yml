name: High Spec - Japan East

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '45 22 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  high-spec-japaneast:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: japaneast
