name: Region (Basic) - Switzerland North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-switzerlandnorth.yml

jobs:
  basic-switzerlandnorth:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: switzerlandnorth
