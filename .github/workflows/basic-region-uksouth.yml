name: Region (Basic) - UK South

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '27 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-uksouth:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: uksouth
