name: Region (Basic) - UK South

permissions:
  id-token: write
  contents: read

on:
  pull_request:
    paths:
      - .github/workflows/basic-region-uksouth.yml
  schedule:
    - cron: '42 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-uksouth:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: uksouth
