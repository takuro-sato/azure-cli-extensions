name: Attestation - Italy North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '16 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  attestation-italynorth:
    secrets: inherit
    uses: ./.github/workflows/workload-attestation.yml
    with:
      location: italynorth
      policy_type: allow_all
      test_schedule: gh-hourly
