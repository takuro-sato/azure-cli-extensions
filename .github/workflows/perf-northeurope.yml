name: Perf - North Europe

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '14 8 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  perf-northeurope:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: northeurope
