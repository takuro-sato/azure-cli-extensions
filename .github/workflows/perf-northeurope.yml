name: Perf - North Europe

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/perf-northeurope.yml

jobs:
  perf-northeurope:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: northeurope
