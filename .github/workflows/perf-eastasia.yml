name: Perf - East Asia

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/perf-eastasia.yml

jobs:
  perf-eastasia:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: eastasia
