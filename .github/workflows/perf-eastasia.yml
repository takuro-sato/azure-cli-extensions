name: Perf - East Asia

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '35 7 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/perf-eastasia.yml

jobs:
  perf-eastasia:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: eastasia
