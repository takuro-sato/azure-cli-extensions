name: Region (Basic) - UAE North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '7,22,37,52 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-uaenorth:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: uaenorth
