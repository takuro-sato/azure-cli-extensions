name: Region (Basic) - UAE North

permissions:
  id-token: write
  contents: read

on:
  pull_request:
    paths:
      - .github/workflows/basic-region-uaenorth.yml
  schedule:
    - cron: '41 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-uaenorth:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: uaenorth
