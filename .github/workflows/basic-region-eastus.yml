name: Region (Basic) - East US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-eastus.yml

jobs:
  basic-eastus:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: eastus
