name: Region (Basic) - East US 2

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '20 2 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-eastus2.yml

jobs:
  basic-eastus2:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: eastus2
