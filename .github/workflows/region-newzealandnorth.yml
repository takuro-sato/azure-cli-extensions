name: Region - New Zealand North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '45 11 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  newzealandnorth:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: newzealandnorth
