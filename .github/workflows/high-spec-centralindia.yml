name: High Spec - Central India

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '35 6 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  high-spec-centralindia:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: centralindia
