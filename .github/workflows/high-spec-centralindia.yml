name: High Spec - Central India

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/high-spec-centralindia.yml

jobs:
  high-spec-centralindia:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: centralindia
