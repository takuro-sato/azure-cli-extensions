name: Region (Basic) - West Europe

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '8,23,38,53 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-westeurope:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: westeurope
