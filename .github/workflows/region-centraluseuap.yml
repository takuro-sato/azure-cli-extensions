name: Region - Central US EUAP

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '56 3 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  centraluseuap:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: centraluseuap
