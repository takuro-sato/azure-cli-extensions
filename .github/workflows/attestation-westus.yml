name: Attestation - West US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '34 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  attestation-westus:
    secrets: inherit
    uses: ./.github/workflows/workload-attestation.yml
    with:
      location: westus
      policy_type: allow_all
      test_schedule: gh-hourly
