name: Perf - West US 2

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '50 3 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  perf-westus2:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: westus2
