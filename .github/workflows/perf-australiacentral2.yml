name: Perf - Australia Central 2

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '41 8 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  perf-australiacentral2:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: australiacentral2
