name: Perf - Australia Central 2

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '22 2 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  perf-australiacentral2:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: australiacentral2
