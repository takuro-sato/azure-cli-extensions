name: Region - West US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 15 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  westus:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: westus
