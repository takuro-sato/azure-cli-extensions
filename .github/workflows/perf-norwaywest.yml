name: Perf - Norway West

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '14 3 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  perf-norwaywest:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: norwaywest
