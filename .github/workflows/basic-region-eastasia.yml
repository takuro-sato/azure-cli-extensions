name: Region (Basic) - East Asia

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-eastasia.yml

jobs:
  basic-eastasia:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: eastasia
