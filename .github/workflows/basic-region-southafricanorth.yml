name: Region (Basic) - South Africa North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/basic-region-southafricanorth.yml

jobs:
  basic-southafricanorth:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: southafricanorth
