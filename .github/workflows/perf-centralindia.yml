name: Perf - Central India

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '42 8 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  perf-centralindia:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: centralindia
