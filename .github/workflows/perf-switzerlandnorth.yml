name: Perf - Switzerland North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '35 7 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/perf-switzerlandnorth.yml

jobs:
  perf-switzerlandnorth:
    secrets: inherit
    uses: ./.github/workflows/perf-region.yml
    with:
      location: switzerlandnorth
