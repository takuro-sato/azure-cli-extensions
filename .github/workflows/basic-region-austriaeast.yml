name: Region (Basic) - Austria East

permissions:
  id-token: write
  contents: read

on:
  pull_request:
    paths:
      - .github/workflows/basic-region-austriaeast.yml
  schedule:
    - cron: '3 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-austriaeast:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: austriaeast
