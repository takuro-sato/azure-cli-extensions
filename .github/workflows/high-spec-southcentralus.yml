name: High Spec - South Central US

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '51 22 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  high-spec-southcentralus:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: southcentralus
