name: Region - Germany North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '36 8 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  germanynorth:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: germanynorth
