name: VM - North Europe

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '55 6 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/vm-northeurope.yml

jobs:
  vm-northeurope:
    secrets: inherit
    uses: ./.github/workflows/vm-region.yml
    with:
      location: northeurope
