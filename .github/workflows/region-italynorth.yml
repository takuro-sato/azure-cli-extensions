name: Region - Italy North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '7 3 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  italynorth:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: italynorth
