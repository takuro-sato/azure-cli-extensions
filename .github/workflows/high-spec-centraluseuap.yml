name: High Spec - Central US EUAP

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '40 22 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  high-spec-centraluseuap:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: centraluseuap
