name: Uptime - South Africa West

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '50 21 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  uptime:
    secrets: inherit
    uses: ./.github/workflows/uptime.yml
    with:
      location: southafricawest
