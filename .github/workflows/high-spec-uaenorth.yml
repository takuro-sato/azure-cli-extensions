name: High Spec - UAE North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '43 23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  high-spec-uaenorth:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: uaenorth
