name: VM - Sweden Central

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/vm-swedencentral.yml

jobs:
  vm-swedencentral:
    secrets: inherit
    uses: ./.github/workflows/vm-region.yml
    with:
      location: swedencentral
