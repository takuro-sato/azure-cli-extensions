name: Region (Basic) - North Europe

permissions:
  id-token: write
  contents: read

on:
  pull_request:
    paths:
      - .github/workflows/basic-region-northeurope.yml
  schedule:
    - cron: '25 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-northeurope:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: northeurope
