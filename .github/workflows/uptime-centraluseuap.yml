name: Uptime - Central US EUAP

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '53 20 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  uptime:
    secrets: inherit
    uses: ./.github/workflows/uptime.yml
    with:
      location: centraluseuap
