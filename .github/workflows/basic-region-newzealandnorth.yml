name: Region (Basic) - New Zealand North

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '14,29,44,59 1-23 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  basic-newzealandnorth:
    secrets: inherit
    uses: ./.github/workflows/basic-region.yml
    with:
      location: newzealandnorth
