name: High Spec - West Europe

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/high-spec-westeurope.yml

jobs:
  high-spec-westeurope:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: westeurope
