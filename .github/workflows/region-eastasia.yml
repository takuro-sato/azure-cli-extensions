name: Region - East Asia

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '4 2 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  eastasia:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: eastasia
