name: Region - Austria East

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '20 2 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  austriaeast:
    secrets: inherit
    uses: ./.github/workflows/region.yml
    with:
      location: austriaeast
