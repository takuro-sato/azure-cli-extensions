name: High Spec - South Africa West

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '57 22 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  high-spec-southafricawest:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: southafricawest
