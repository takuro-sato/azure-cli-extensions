name: High Spec - Germany West Central

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '19 21 * * *'  # managed by scripts/spread_out_schedules.py
  workflow_dispatch:

jobs:
  high-spec-germanywestcentral:
    uses: ./.github/workflows/high-spec.yml
    secrets: inherit
    with:
      location: germanywestcentral
